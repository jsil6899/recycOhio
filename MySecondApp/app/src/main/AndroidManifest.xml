<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jenni.mysecondapp">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DisplayMessageActivity" />
        <activity android:name=".DisplayBottles" />
        <activity
            android:name=".DisplayFoods"
            android:label="@string/title_activity_display_foods" />
        <activity android:name=".DisplayLiquids" />
        <activity android:name=".DisplayPlastic" />
        <activity android:name=".DisplayOther" />
        <activity android:name=".WaterBottleYes" />
        <activity android:name=".CoffeeCups" />
        <activity android:name=".GlassBottles" />
        <activity android:name=".SodaCans" />
        <activity
            android:name=".PizzaBoxes"
            android:label="@string/title_activity_pizza_boxes" />
        <activity android:name=".CerealBoxes" />
        <activity android:name=".ToGoBoxes" />
        <activity android:name=".ChipBags" />
        <activity android:name=".one" />
        <activity android:name=".three" />
        <activity android:name=".five" />
        <activity android:name=".seven" />
        <activity android:name=".tech" />
        <activity android:name=".appliances" />
        <activity android:name=".shoppingBags" />
        <activity android:name=".paper" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ApplianceMap"
            android:label="@string/title_activity_appliance_map" />
        <activity android:name=".Liquids2" />
        <activity android:name=".shampoo" />
        <activity android:name=".detergent" />
        <activity android:name=".solution" />
        <activity android:name=".jugs" />
        <activity
            android:name=".TechMap"
            android:label="@string/title_activity_tech_map"></activity>
    </application>

</manifest>